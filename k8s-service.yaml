apiVersion: v1
kind: Service
metadata:
  name: calculator-api-service
spec:
  type: NodePort
  selector:
    app: calculator-api
  ports:
    - protocol: TCP
      port: 5000         # Port inside the cluster
      targetPort: 5000   # Port on the pod/container
      nodePort: 30050    # Port exposed on your machine/node