apiVersion: v1
kind: Service
metadata:
  name: calculator-api-service
spec:
  type: NodePort
  selector:
    app: calculator-api
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30050